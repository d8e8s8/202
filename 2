# This program calculates the average of numbers in a list

numbers = [10, 25, 5, 45, 60, 20] # Define a list of numbers
average = sum(numbers) / len(numbers) # Calculate the average

print("The average is:", average) # Output the average
